# go-webapp

1. Clone the repository to your local machine
2. Build the Docker Image
3. Run the Docker image
  ```sh
  docker run -p 8080:8080 gowebapp
  ```
4. Go to http://localhost:8080 or http://localhost:8080/world
